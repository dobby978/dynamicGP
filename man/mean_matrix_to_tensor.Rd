% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/average_matrix_tensor.R
\name{mean_matrix_to_tensor}
\alias{mean_matrix_to_tensor}
\title{Broadcast a Matrix to a 3-D Tensor with Identical Slices}
\usage{
mean_matrix_to_tensor(avg_matrix, n, line_names = NULL)
}
\arguments{
\item{avg_matrix}{A numeric matrix \eqn{[p \times q]} to broadcast.  Row
and column names, if present, are propagated to the first two dimensions
of the output tensor.}

\item{n}{Positive integer.  Number of slices (genotypes/samples) in the
output tensor.}

\item{line_names}{Character vector of length \code{n} providing names for the
third dimension (e.g. genotype IDs).  \code{NULL} (default) leaves the third
dimension unnamed.}
}
\value{
A 3-D numeric array of dimensions \eqn{[p \times q \times n]}
where every third-dimension slice equals \code{avg_matrix}.  \code{dimnames} on
the first two dimensions are taken from \code{avg_matrix}; the third dimension
is named by \code{line_names}.
}
\description{
Creates a 3-D array \eqn{[p \times q \times n]} where every slice along
the third dimension is a copy of \code{avg_matrix}.  This is the inverse
operation to \code{\link[=tensor_to_mean_matrix]{tensor_to_mean_matrix()}}: given a mean or baseline matrix,
it produces a constant tensor suitable for element-wise comparison or
combination with a per-genotype tensor.
}
\examples{
set.seed(1)
A_mean   <- matrix(rnorm(16), 4, 4,
                   dimnames = list(paste0("T", 1:4), paste0("T", 1:4)))
A_tensor <- mean_matrix_to_tensor(A_mean, n = 20,
                                  line_names = paste0("G", 1:20))
dim(A_tensor)              # 4 x 4 x 20
all(A_tensor[, , 1] == A_mean)   # TRUE

}
\seealso{
\code{\link[=tensor_to_mean_matrix]{tensor_to_mean_matrix()}}
}
