% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/greml_heritability.R
\name{greml_heritability}
\alias{greml_heritability}
\title{Compute GREML SNP-based Heritability}
\usage{
greml_heritability(
  matrix_unrolled,
  kinship_matrix = NULL,
  kinship_filename = NULL,
  backend = c("r", "gcta"),
  gcta_bin = "gcta64",
  run_name = "greml_temp",
  save_run = FALSE,
  save_filename = NULL,
  plot = TRUE,
  progress_bar = FALSE,
  verbose = FALSE,
  tag = ""
)
}
\arguments{
\item{matrix_unrolled}{A numeric data frame or matrix with \eqn{G} rows
(genotypes) and \eqn{q} columns (traits).  Row names must be genotype
identifiers matching the row/column names of \code{kinship_matrix}.  This is
typically the output of \code{\link[=arr_to_table]{arr_to_table()}}.}

\item{kinship_matrix}{A square numeric matrix or data frame of dimension
\eqn{G \times G} containing the genomic relationship matrix (GRM).  Row
and column names should be the same genotype identifiers as
\code{rownames(matrix_unrolled)}.  Required for \code{backend = "r"}.  For
\code{backend = "gcta"} either this or \code{kinship_filename} must be provided.}

\item{kinship_filename}{Character.  Path prefix to a pre-existing GCTA
binary GRM (\code{.grm.bin}, \code{.grm.N.bin}, \code{.grm.id}).  Only
used when \code{backend = "gcta"}.  If \code{kinship_matrix} is provided instead,
the binary files are written to \code{run_name} automatically using
\pkg{plinkFile}.}

\item{backend}{Character; one of \code{"r"} (default) or \code{"gcta"}.  Controls
which computation engine is used.  See the \emph{Backend choice} section.}

\item{gcta_bin}{Character.  Name or full path of the GCTA executable.
Only used when \code{backend = "gcta"}.  Defaults to \code{"gcta64"} (assumes GCTA
is on the system \code{PATH}).  Set to the absolute path if needed, e.g.
\code{gcta_bin = "/usr/local/bin/gcta64"}.}

\item{run_name}{Character.  Name of the temporary working directory created
when \code{backend = "gcta"}.  Must be a plain directory name with no path
separators (e.g. \code{"greml_temp"}, not \code{"../greml_temp"}).  Ignored for
\code{backend = "r"}.  Default \code{"greml_temp"}.}

\item{save_run}{Logical.  When \code{TRUE} the temporary GCTA directory (and its
\code{.hsq} files) is kept after the run.  Default \code{FALSE}.}

\item{save_filename}{Character or \code{NULL}.  If a file path is provided the
results data frame is written there as a CSV.}

\item{plot}{Logical.  When \code{TRUE} a histogram of \eqn{\hat{h}^2} values is
attached to the return value as \code{attr(result, "plot")}.  Default \code{TRUE}.}

\item{progress_bar}{Logical.  Show a trait-level progress bar.  Default
\code{FALSE}.}

\item{verbose}{Logical.  When \code{TRUE}, GCTA console output is echoed and
gaston convergence messages are shown.  Default \code{FALSE}.}

\item{tag}{Optional character label appended to progress bar and plot
titles (e.g. an environment or time-period name).  Default \code{""}.}
}
\value{
A \code{data.frame} with one row per trait and columns:
\describe{
\item{\code{trait}}{Trait name (from \code{colnames(matrix_unrolled)}).}
\item{\code{h2}}{GREML estimate of \eqn{h^2_{SNP}}.}
\item{\code{se_h2}}{Standard error of \eqn{\hat{h}^2}. For \code{backend = "r"}
this is computed via the delta method from the AI matrix; for
\code{backend = "gcta"} it is taken directly from the \code{.hsq} file.}
\item{\code{V_G}}{Estimated genetic variance \eqn{\hat{\sigma}^2_g}.}
\item{\code{V_e}}{Estimated residual variance \eqn{\hat{\sigma}^2_e}.}
\item{\code{V_p}}{Total phenotypic variance \eqn{\hat{\sigma}^2_g +
    \hat{\sigma}^2_e}.}
}
When \code{plot = TRUE}, the ggplot2 histogram is attached as
\code{attr(result, "plot")}.
}
\description{
Estimates narrow-sense SNP heritability (\eqn{h^2_{SNP}}) for each column
of \code{matrix_unrolled} using Genomic REML (GREML).  Two backends are
available: a pure-R implementation via the \pkg{gaston} package
(\code{backend = "r"}, default) and an optional GCTA binary backend
(\code{backend = "gcta"}).
}
\section{Model}{

For each trait \eqn{y}, the univariate mixed model
\deqn{y = 1\mu + u + e, \quad u \sim \mathcal{N}(0, G\sigma^2_g),
       \quad e \sim \mathcal{N}(0, I\sigma^2_e)}
is fitted by REML.  Heritability is then
\eqn{h^2 = \sigma^2_g / (\sigma^2_g + \sigma^2_e)}.
}

\section{Backend choice}{

\describe{
\item{\code{"r"} (default)}{Runs AI-REML via \code{\link[gaston]{lmm.aireml}}.
No external software needed.  Install with
\code{install.packages("gaston")}.  Fastest for \eqn{n < 3\,000}.}
\item{\code{"gcta"}}{Calls the GCTA binary (usually \code{gcta64}) via
\code{system()}.  Requires GCTA to be installed on your system and the
path supplied via \code{gcta_bin}.  Advantageous only when
\eqn{n > 5\,000}.  See \url{https://yanglab.westlake.edu.cn/software/gcta/}.}
}
}

\examples{
\dontrun{
# ── R backend (default) ──────────────────────────────────────────────────
# 'unrolled' is a [G x q] matrix from arr_to_table()
# 'K' is a [G x G] genomic relationship matrix
h2_df <- greml_heritability(unrolled, kinship_matrix = K)
attr(h2_df, "plot")   # view the histogram

# ── GCTA backend (opt-in) ────────────────────────────────────────────────
h2_df <- greml_heritability(
  unrolled,
  kinship_matrix = K,
  backend  = "gcta",
  gcta_bin = "/usr/local/bin/gcta64"
)
}

}
\seealso{
\code{\link[=arr_to_table]{arr_to_table()}}
}
