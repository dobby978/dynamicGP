% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_arrays}
\alias{compare_arrays}
\title{Compare Two 3-D State Arrays}
\usage{
compare_arrays(
  X_arr,
  X_pred_arr,
  rv_by_trait = FALSE,
  rv_by_geno = FALSE,
  use_dtw = TRUE
)
}
\arguments{
\item{X_arr}{Observed array \eqn{[p \times N \times G]}, or a
\eqn{[p \times N]} matrix (single genotype, \code{G = 1}).}

\item{X_pred_arr}{Predicted array with the same \code{p} and \code{G} dimensions.
If the \code{N} dimension differs, both arrays are truncated to the shorter
length.}

\item{rv_by_trait}{Logical.  Compute per-trait RV coefficients.
Default \code{FALSE}.}

\item{rv_by_geno}{Logical.  Compute per-genotype RV coefficients.
Default \code{FALSE}.}

\item{use_dtw}{Logical.  Compute DTW distances using the \pkg{dtw} package
(listed in \code{Suggests}).  Silently skipped with a message if the package
is not installed.  Default \code{TRUE}.}
}
\value{
A named list with two mandatory elements (\code{snapshot},
\code{longitudinal}) and up to two optional elements:
\describe{
\item{\code{snapshot}}{Data frame: \code{trait}, \code{time_step}, \code{n_obs}, \code{pcc},
\code{pcc_pval}, \code{mse}.}
\item{\code{longitudinal}}{Data frame: \code{trait}, \code{genotype}, \code{n_obs}, \code{pcc},
\code{pcc_pval}, \code{spearman}, \code{spearman_pval}, \code{mse}, \code{dtw_dist} (normalised
DTW distance in \eqn{[0, 1]}, divided by warping path length).}
\item{\code{rv_by_trait}}{Named numeric vector (present when
\code{rv_by_trait = TRUE}).}
\item{\code{rv_by_geno}}{Named numeric vector (present when
\code{rv_by_geno = TRUE}).}
}
}
\description{
Computes prediction accuracy between an observed array (e.g. \code{X_arr}) and
a predicted array (e.g. \code{X_pred_arr} from
\code{\link[=reconstruct_dmd]{reconstruct_dmd()}}) along three complementary axes.
}
\section{Accuracy types}{

\describe{
\item{\strong{Snapshot accuracy} (\verb{$snapshot})}{For each (trait, time-point)
pair: the similarity \emph{across genotypes} between observed and predicted
values.  Metrics: Pearson correlation (PCC) with p-value and MSE.
At least three genotypes are required for a non-trivial PCC.}
\item{\strong{Longitudinal accuracy} (\verb{$longitudinal})}{For each (trait,
genotype) pair: the similarity \emph{over time} between the observed and
predicted trajectory.  Metrics: PCC, Spearman correlation (both with
p-values), MSE, and optionally DTW distance.}
\item{\strong{RV by trait} (\verb{$rv_by_trait}, optional)}{For each trait: the RV
coefficient (Robert & Escoufier 1976) between the observed and predicted
\eqn{[G \times N]} matrices -- a multivariate similarity measure.}
\item{\strong{RV by genotype} (\verb{$rv_by_geno}, optional)}{For each genotype:
the RV coefficient between the observed and predicted
\eqn{[N \times p]} matrices.}
}
}

\references{
Robert, P. & Escoufier, Y. (1976). A unifying tool for linear multivariate
statistical methods: the RV coefficient. \emph{Applied Statistics}, 25(3),
257--265. \doi{10.2307/2347233}

Sakoe, H. & Chiba, S. (1978). Dynamic programming algorithm optimization
for spoken word recognition. \emph{IEEE Transactions on Acoustics, Speech, and
Signal Processing}, 26(1), 43--49.
}
\seealso{
\code{\link[=reconstruct_dmd]{reconstruct_dmd()}}, \code{\link[=plot_trajectories]{plot_trajectories()}}
}
