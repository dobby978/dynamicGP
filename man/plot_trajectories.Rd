% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectories.R
\name{plot_trajectories}
\alias{plot_trajectories}
\title{Plot Trait Trajectories Over Time}
\usage{
plot_trajectories(
  X_list,
  traits = NULL,
  time_index = NULL,
  show_individuals = FALSE,
  facet_by_trait = FALSE,
  save_plots = FALSE,
  plots_dir = ".",
  plot_format = "png",
  width = 10,
  height = 6
)
}
\arguments{
\item{X_list}{A 3-D numeric array \eqn{[p \times N \times G]}, a
\eqn{[p \times N]} matrix (single genotype), or a \strong{named list} of such
arrays.  Unnamed list elements are labelled \code{"Source 1"}, \code{"Source 2"},
etc.}

\item{traits}{Optional character vector of trait names to plot.  Must match
\code{dimnames(X_list)[[1]]} (or \code{dimnames(X_list[[1]])[[1]]} for a list).
\code{NULL} (default) plots all traits.}

\item{time_index}{Optional numeric vector of length \code{N} used as the x-axis
values.  Defaults to integer indices \verb{1, 2, ..., N}.}

\item{show_individuals}{Logical.  Draw a thin, semi-transparent line for
each individual genotype in addition to the mean.  Default \code{FALSE}.}

\item{facet_by_trait}{Logical.  If \code{TRUE} and the number of selected traits
is fewer than 30, produce a single faceted plot rather than one plot per
trait.  Default \code{FALSE}.}

\item{save_plots}{Logical.  Save all plots to \code{plots_dir}.  Default \code{FALSE}.}

\item{plots_dir}{Character.  Output directory (created recursively if
absent).  Default \code{"."}.}

\item{plot_format}{Character file format: \code{"png"}, \code{"svg"}, or \code{"pdf"}.
Default \code{"png"}.}

\item{width, height}{Plot dimensions in inches.  Defaults \code{10} and \code{6}.}
}
\value{
Invisibly returns a named list of \pkg{ggplot2} objects.  Names
are trait names, or \code{"faceted"} for a faceted plot.
}
\description{
Creates mean-trajectory plots (line \eqn{\pm} 1 SD ribbon) for one or more
traits across genotypes.  Accepts a single 3-D array \strong{or} a named list
of 3-D arrays (e.g. observed and predicted), which are then overlaid on
the same axes with a colour legend.
}
\examples{
\dontrun{
# Overlay observed and reconstructed trajectories
result <- run_dmd(X_array[, -ncol(X_array)], X_array[, -1L])
pred   <- reconstruct_dmd(result$A_arr, X_obs = result$X_arr)
plot_trajectories(
  list(Observed = result$X_arr, Predicted = pred$X_pred_arr),
  show_individuals = TRUE,
  facet_by_trait   = TRUE
)
}

}
\seealso{
\code{\link[=reconstruct_dmd]{reconstruct_dmd()}}, \code{\link[=compare_arrays]{compare_arrays()}}
}
