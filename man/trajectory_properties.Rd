% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectory_properties.R
\name{trajectory_properties}
\alias{trajectory_properties}
\title{Compute Trajectory Properties for 3-D Phenotype Arrays}
\usage{
trajectory_properties(
  X_list,
  plot = FALSE,
  plot_type = c("boxplot", "violin", "histogram"),
  metrics = NULL,
  facet_arrays = FALSE,
  save_plot = FALSE,
  plots_dir = NULL,
  width = 10,
  height = 8
)
}
\arguments{
\item{X_list}{A 3-D numeric array \eqn{[p \times N \times G]} \strong{or} a
named list of such arrays (one per dataset / environment / period).  A
single unnamed array is wrapped into a one-element list labelled
\code{"arr1"}.  Each array element may also be a 2-D matrix, promoted to
\eqn{[p \times N \times 1]} internally.}

\item{plot}{Logical.  When \code{TRUE}, a ggplot2 figure is produced and
returned as an attribute (accessible via \code{attr(result, "plot")}).
Default \code{FALSE}.}

\item{plot_type}{Character; one of \code{"boxplot"} (default), \code{"violin"}, or
\code{"histogram"}.  Ignored when \code{plot = FALSE}.}

\item{metrics}{Character vector naming which metrics to include in the plot.
Default \code{NULL} uses all metrics except \code{n_obs}.}

\item{facet_arrays}{Logical.  When \code{TRUE} \strong{and} \code{X_list} has more than
one element, the plot is faceted by both metric and array.  When \code{FALSE}
(default) arrays are distinguished by fill colour.  Ignored when
\code{plot = FALSE}.}

\item{save_plot}{Logical.  When \code{TRUE} the plot is saved to \code{plots_dir}.
Default \code{FALSE}.}

\item{plots_dir}{Character path to the output directory for saved plots.
Required when \code{save_plot = TRUE}.}

\item{width, height}{Plot dimensions in inches (default 10 × 8).}
}
\value{
A \code{data.frame} with columns \code{array}, \code{trait}, \code{genotype}, and one
column per metric listed above.  When \code{plot = TRUE} the plot object is
attached as \code{attr(result, "plot")}.
}
\description{
For every combination of array × trait × genotype, extracts the univariate
time series and computes a suite of trajectory characteristics.  The result
is a tidy data frame — one row per (array, trait, genotype) — that can be
used directly for downstream modelling or visualised with the optional
built-in plots.
}
\section{Metrics computed}{

\describe{
\item{\code{total_variation}}{Sum of absolute first differences
\eqn{\sum_t |x_{t+1} - x_t|} — total distance travelled.}
\item{\code{quadratic_variation}}{Variance of first differences
\eqn{\mathrm{Var}(\Delta x)} — irregularity of increments.}
\item{\code{cv}}{Coefficient of variation \eqn{\sigma / |\mu|} — relative
spread.  \code{NA} when the trajectory mean is zero.}
\item{\code{rms_roughness}}{\eqn{\sqrt{\mathrm{mean}(\Delta x^2)}} —
un-centred RMS of increments; measures typical step magnitude.}
\item{\code{kurtosis}}{4th standardised central moment
\eqn{\mathrm{E}[(x - \mu)^4] / \sigma^4} (Gaussian = 3).}
\item{\code{mean_second_diff}}{Mean of the second-order differences
\eqn{\mathrm{mean}(\Delta^2 x)} — average acceleration/curvature.}
\item{\code{quadratic_coef}}{Coefficient of \eqn{t^2} from OLS fit
\eqn{x \sim 1 + t + t^2} — non-linearity of the trend.}
\item{\code{mean_chord_dev}}{Mean absolute deviation from the straight-line
chord connecting the first and last observed time points.}
\item{\code{trend_slope}}{Slope from OLS fit \eqn{x \sim 1 + t} — linear
trend per unit time step.}
\item{\code{lag1_acf}}{Lag-1 autocorrelation \eqn{\rho(1) = \mathrm{cor}(x_t,
    x_{t+1})} — temporal persistence.}
\item{\code{skewness}}{3rd standardised central moment
\eqn{\mathrm{E}[(x-\mu)^3] / \sigma^3} — asymmetry.}
\item{\code{range}}{\eqn{\max(x) - \min(x)} — peak-to-peak amplitude.}
\item{\code{n_obs}}{Number of finite (non-\code{NA}) observations used.}
}
}

\examples{
set.seed(42)
p <- 3L; N <- 10L; G <- 5L
X <- array(rnorm(p * N * G), dim = c(p, N, G),
           dimnames = list(paste0("T", 1:p),
                           paste0("t", 1:N),
                           paste0("G", 1:G)))
df <- trajectory_properties(X)
head(df)

# Multiple arrays (e.g. stress vs. stable environment)
X2 <- array(rnorm(p * N * G), dim = c(p, N, G),
            dimnames = dimnames(X))
df2 <- trajectory_properties(list(stress = X, stable = X2), plot = TRUE)
attr(df2, "plot")

}
\seealso{
\code{\link[=concat_arrays]{concat_arrays()}}
}
