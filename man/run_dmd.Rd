% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmd.R
\name{run_dmd}
\alias{run_dmd}
\title{Dynamic Mode Decomposition (DMD)}
\usage{
run_dmd(
  X1,
  X2,
  r = NULL,
  var_thresh = 0.95,
  use_pinv = FALSE,
  break_after = integer(0L),
  method = c("direct", "standard", "schur-dmd"),
  dt_days = 1
)
}
\arguments{
\item{X1}{Numeric matrix of shape \verb{p × m}: state snapshots at times
\verb{t = 1, ..., m}.}

\item{X2}{Numeric matrix of shape \verb{p × m}: state snapshots at times
\verb{t = 2, ..., m+1} (one step ahead of \code{X1}).}

\item{r}{Integer DMD rank.  \code{NULL} (default) triggers automatic selection
based on \code{var_thresh}.  Ignored when \code{method = "direct"}.}

\item{var_thresh}{Cumulative-variance threshold for automatic rank selection
(default \code{0.95}).  The smallest \code{r} such that the leading \code{r} singular
values of \code{X1} explain at least \code{var_thresh} of total variance is used.
Ignored when \code{method = "direct"}.}

\item{use_pinv}{Deprecated.  Use \code{method = "direct"} instead.}

\item{break_after}{Integer vector of 1-based column indices of \code{X1} after
which a time-series gap occurs.  The snapshot pair \verb{(X1[,k], X2[,k])} is
excluded so that cross-gap transitions do not enter the regression.
Default \code{integer(0)}.}

\item{method}{Character string: \code{"direct"} (full pseudoinverse),
\code{"standard"} (default), or \code{"schur-dmd"} (Schur decomposition).}

\item{dt_days}{Timestep in days, used only to convert discrete eigenvalues
\eqn{\lambda} to continuous-time frequencies and growth rates via
\eqn{\omega = \log(\lambda) / \Delta t}.  Default \code{1} (eigenvalues
reported in per-timestep units).}
}
\value{
A named list with components:
\describe{
\item{\code{A_full}}{Full p \eqn{\times} p state-transition matrix.}
\item{\code{A_til}}{Reduced r \eqn{\times} r operator (NULL when \code{use_pinv = TRUE}).}
\item{\code{B_full}, \code{B_til}}{\code{NULL} (no control inputs for plain DMD).}
\item{\code{lam}}{Complex vector of \code{r} DMD eigenvalues.}
\item{\code{Phi}}{Complex \verb{p × r} matrix of DMD modes.}
\item{\code{omega_c}}{Continuous-time eigenvalues \eqn{\log(\lambda)/\Delta t}.}
\item{\code{freq_cpd}}{Frequencies in cycles per day.}
\item{\code{growth_day}}{Growth/decay rates per day.}
\item{\code{amp}}{Mode amplitudes.}
\item{\code{rho_A}}{Spectral radius of \code{A_full}.}
\item{\code{resid_F}}{Relative Frobenius residual
\eqn{\|X_2 - A X_1\|_F / \|X_2\|_F}.}
\item{\code{r}, \code{eps}}{Chosen output rank (eps = r for DMD).}
\item{\code{pv_om}, \code{pv_x2}}{Cumulative variance explained by the SVD of
\code{X1} and \code{X2} respectively.}
\item{\code{method}}{Character tag: \code{"direct"}, \code{"standard"}, or \code{"schur-dmd"}.}
}
}
\description{
Fits the linear model \eqn{\mathbf{x}(t+1) = A \cdot \mathbf{x}(t)} by
SVD-truncated regression or full pseudoinverse.
}
\details{
Two decomposition methods are available:
\describe{
\item{\code{"direct"}}{Full pseudoinverse.  Computes \eqn{A = X_2 X_1^+} via
\code{pracma::pinv()} with no rank truncation.  The rank of the result equals
the numerical rank of \code{X1}.}
\item{\code{"standard"}}{Standard exact DMD.  Computes the reduced operator
\eqn{\tilde{A} = \hat{U}^T X_2 \hat{V} \hat{\Sigma}^{-1}} and extracts
eigenvectors \eqn{W} and eigenvalues \eqn{\lambda} via \code{eigen()}.
Modes are lifted back to full space:
\eqn{\Phi = X_2 \hat{V} \hat{\Sigma}^{-1} W / \lambda}.}
\item{\code{"schur-dmd"}}{Same \eqn{\tilde{A}}, but decomposed via the real Schur
form \eqn{\tilde{A} = Q T Q^T} using \code{\link[Matrix:Schur]{Matrix::Schur()}}.  More stable
when eigenvalues are nearly degenerate.  Falls back to \code{eigen()} with a
warning if the \strong{Matrix} package is unavailable.}
}
}
\references{
Schmid, P.J. (2010). Dynamic mode decomposition of numerical and
experimental data. \emph{Journal of Fluid Mechanics}, 656, 5–28.
\doi{10.1017/S0022112010001217}

Tu, J.H. \emph{et al.} (2014). On dynamic mode decomposition: theory and
applications. \emph{Journal of Computational Dynamics}, 1(2), 391–421.
\doi{10.3934/jcd.2014.1.391}
}
\seealso{
\code{\link[=reconstruct_dmd]{reconstruct_dmd()}}
}
