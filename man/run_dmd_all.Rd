% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_dmd_all.R
\name{run_dmd_all}
\alias{run_dmd_all}
\title{Fit DMD Models Across Multiple Genotypes (or Samples)}
\usage{
run_dmd_all(
  X_array = NULL,
  ts_list = NULL,
  trait_names = NULL,
  var_thresh = 0.95,
  r = NULL,
  return_intermediates = TRUE,
  break_after = NULL,
  norm_method = "minmax",
  norm_stats = NULL,
  method = "standard",
  irregular_handling = "error"
)
}
\arguments{
\item{X_array}{Numeric array \verb{[p × N]} or \verb{[p × N × G]} of state
(trait) snapshots.  Traits are rows, time is columns, genotypes are the
third dimension.  \code{dimnames(X_array)[[1]]} (trait names) and
\code{dimnames(X_array)[[3]]} (genotype names) are used when present.}

\item{ts_list}{\code{POSIXct} vector or named list of vectors giving the
timestamp for each column of \code{X_array}.  \code{NULL} falls back to integer
indices.}

\item{trait_names}{Optional character vector of trait display names.
Falls back to \code{dimnames(X_array)[[1]]}.}

\item{var_thresh}{Cumulative-variance threshold for automatic SVD rank
selection (default \code{0.95}).}

\item{r}{Integer output rank override.  \code{NULL} (default) → auto.
Ignored when \code{method = "direct"}.}

\item{return_intermediates}{Logical.  If \code{TRUE} (default) also return
reduced-space intermediate matrices (Phi, A_til, U_hat, V_r, sig_inv)
as 3-D arrays in \verb{$inter}.}

\item{break_after}{Gap specification.  Can be:
\describe{
\item{\code{NULL}}{No exclusions (default).}
\item{integer vector}{Same indices applied to all genotypes.}
\item{named list}{Per-genotype integer vectors,
e.g. \code{list(D04b = c(480L, 960L), I16b = c(480L))}.}
}
1-based indices in the per-genotype filtered (complete-cases) time
series.  Snapshot pair \verb{(X[,i], X[,i+1])} is excluded.}

\item{norm_method}{Normalisation for state \code{X}: \code{"minmax"} (default),
\code{"zscore"}, or \code{"none"}.}

\item{norm_stats}{Pre-computed normalisation statistics (list with fields
\code{state_mean}, \code{state_sd}, \code{state_min}, \code{state_range}) injected by an
external wrapper when data should be scaled relative to a pooled
reference.  \code{NULL} (default) computes statistics from \code{X_array}.}

\item{method}{Fitting method: \code{"standard"} (default), \code{"schur-dmd"}, or
\code{"direct"}.}

\item{irregular_handling}{Action when irregular time spacing is detected:
\code{"error"} (default, stop with an informative message) or \code{"warn"}
(emit a warning and continue with the chosen \code{method}).}
}
\value{
A named list with five elements:
\describe{
\item{\code{dat}}{Named list of per-genotype result lists, each containing:
timestamps (\code{ts}, \code{ts_days}), normalisation parameters
(\code{norm_method}, \code{state_center}, \code{state_scale}), spectral results
(\code{r}, \code{rho_A}, \code{resid_F}, \code{lam}, \code{omega_c}, \code{freq_cpd},
\code{growth_day}, \code{amp}, \code{Phi}), and metadata (\code{method}, \code{break_after}).}
\item{\code{A_arr}}{3-D array \verb{[p × p × G]} of state-transition matrices.}
\item{\code{X_arr}}{3-D array \verb{[p × N_max × G]} of normalised state data.}
\item{\code{inter}}{List of reduced-space 3-D arrays (Phi, A_til, U_hat,
V_r, sig_inv), or \code{NULL} when \code{return_intermediates = FALSE}.}
}
}
\description{
The DMD-only counterpart of \code{\link[=run_dmdc_all]{run_dmdc_all()}}.  Accepts 2-D (single
genotype) or 3-D (multiple genotypes) state arrays, normalises the data,
and fits a Dynamic Mode Decomposition (DMD) model for each genotype using
the chosen method.
}
\details{
\strong{Input shapes:}
\itemize{
\item \code{X_array}: \verb{[p × N]} (single genotype, promoted internally to
\verb{[p × N × 1]}) or \verb{[p × N × G]} (G genotypes).
\item \code{ts_list}: a \code{POSIXct} vector (single genotype) or named list of
\code{POSIXct} vectors (multiple genotypes).
}

\strong{Normalisation} is applied globally across \emph{all} genotypes before
fitting so that operator magnitudes are directly comparable:
\itemize{
\item \code{"minmax"} (default): map to the range [0, 1] using global min and
range.  Sensitive to outliers; prefer \code{"zscore"} when cross-genotype
magnitude comparability is less important than robustness.
\item \code{"zscore"}: subtract global mean, divide by global SD.
Zero-SD traits are set to 0.
\item \code{"none"}: no normalisation.
}

\strong{Methods} (selected via the \code{method} argument):
\describe{
\item{\code{"standard"} (default)}{SVD-truncated exact DMD.
Fits \eqn{x(t+1) = Ax(t)}.}
\item{\code{"schur-dmd"}}{Schur-decomposition DMD (requires \pkg{Matrix}).
Falls back to \code{"standard"} if \pkg{Matrix} is unavailable.}
\item{\code{"direct"}}{Full pseudoinverse solution (requires \pkg{pracma}).}
}
}
\seealso{
\code{\link[=run_dmd]{run_dmd()}}, \code{\link[=reconstruct_dmd]{reconstruct_dmd()}}, \code{\link[=plot_trajectories]{plot_trajectories()}}
}
