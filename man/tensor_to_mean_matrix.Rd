% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/average_matrix_from_tensor.R
\name{tensor_to_mean_matrix}
\alias{tensor_to_mean_matrix}
\title{Compute the Element-Wise Mean Matrix from a 3-D Array}
\usage{
tensor_to_mean_matrix(tensor, na.rm = TRUE)
}
\arguments{
\item{tensor}{A 3-D numeric array \eqn{[p \times q \times G]}.  The first
two dimensions define the matrix structure; the third dimension
(genotypes/samples) is averaged over.  \code{dimnames} on the first two
dimensions are preserved in the output.}

\item{na.rm}{Logical.  If \code{TRUE} (default), \code{NA} values are excluded when
computing each element mean.  Set to \code{FALSE} to propagate \code{NA} whenever
any slice has a missing entry at that position.}
}
\value{
A numeric matrix of dimensions \eqn{[p \times q]} whose
\eqn{(i,j)} entry is the mean of \code{tensor[i, j, ]}.  Row and column
names are taken from \code{dimnames(tensor)[[1]]} and \code{dimnames(tensor)[[2]]}.
}
\description{
Reduces a 3-D array \eqn{[p \times q \times G]} to a single
\eqn{[p \times q]} matrix by taking the mean across the third
(genotype/sample) dimension at every \eqn{(i, j)} position.
}
\examples{
set.seed(1)
A_arr <- array(rnorm(4 * 4 * 20), dim = c(4, 4, 20),
               dimnames = list(paste0("T", 1:4), paste0("T", 1:4),
                               paste0("G", 1:20)))
A_mean <- tensor_to_mean_matrix(A_arr)
dim(A_mean)   # 4 x 4

}
\seealso{
\code{\link[=mean_matrix_to_tensor]{mean_matrix_to_tensor()}}
}
