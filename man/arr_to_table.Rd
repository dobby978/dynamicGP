% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape.R
\name{arr_to_table}
\alias{arr_to_table}
\title{Flatten a 3-D Array to a 2-D Genotype-by-Entry Data Frame}
\usage{
arr_to_table(arr, sep = "_._._")
}
\arguments{
\item{arr}{A 3-D numeric array \eqn{[d_1 \times d_2 \times G]}, or a 2-D
matrix (treated as a single genotype and returned as a 1-row data frame).}

\item{sep}{Character separator between the two dimension labels in the
column names.  Default \code{"_._._"}.}
}
\value{
A \code{data.frame} with \code{G} rows (genotypes) and \eqn{d_1 \cdot d_2}
columns.  Row names equal the genotype names.
}
\description{
Converts any 3-D array \eqn{[d_1 \times d_2 \times G]} — such as \code{A_arr},
\code{B_arr}, \code{X_arr} — into a 2-D
data frame with one row per genotype and one column per
\eqn{(d_1, d_2)} entry pair.
}
\details{
Column names are formed by pasting the \eqn{d_1} and \eqn{d_2} dimnames
together with \code{sep}.  When dimnames are absent, fallback labels
\code{"d1_<i>"} / \code{"d2_<j>"} are used.  Row names are the genotype names from
\code{dimnames(arr)[[3]]}.

The vectorisation order matches R's column-major convention: for a
\eqn{d_1 \times d_2} slice, element \eqn{(i, j)} appears at position
\eqn{(j - 1) \cdot d_1 + i}.
}
\examples{
# Flatten A_arr [4 x 4 x 3] -> data frame [3 x 16]
A <- array(rnorm(4 * 4 * 3), dim = c(4, 4, 3),
           dimnames = list(paste0("T", 1:4), paste0("T", 1:4),
                           paste0("G", 1:3)))
df <- arr_to_table(A)
dim(df)   # 3 x 16

}
\seealso{
\code{\link[=table_to_arr]{table_to_arr()}}
}
