% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape.R
\name{table_to_arr}
\alias{table_to_arr}
\title{Reconstruct a 3-D Array from a 2-D Genotype-by-Entry Table}
\usage{
table_to_arr(
  tbl,
  d1 = NULL,
  d2 = NULL,
  sep = "_._._",
  dim1_names = NULL,
  dim2_names = NULL,
  geno_names = NULL
)
}
\arguments{
\item{tbl}{A data frame or matrix with \code{G} rows and \eqn{d_1 \cdot d_2}
columns.  Row names are used as genotype names when available.}

\item{d1}{Integer: size of the first array dimension.  When \code{NULL}
(default) it is inferred from the number of unique first-part labels
obtained by splitting \code{colnames(tbl)} at \code{sep}.}

\item{d2}{Integer: size of the second array dimension.  When \code{NULL}
(default) it is inferred analogously from the unique second-part labels.}

\item{sep}{Character separator used to split column names into their
\eqn{(d_1, d_2)} label components.  Must match the \code{sep} used when
\code{\link[=arr_to_table]{arr_to_table()}} produced the table.  Default \code{"_._._"}.}

\item{dim1_names}{Optional character vector of length \code{d1} for
\code{dimnames(arr)[[1]]}.  When \code{NULL} and \code{sep}-splitting succeeds, the
unique first-part labels are used automatically.}

\item{dim2_names}{Optional character vector of length \code{d2} for
\code{dimnames(arr)[[2]]}.  When \code{NULL} and \code{sep}-splitting succeeds, the
unique second-part labels are used automatically.}

\item{geno_names}{Optional character vector of length \code{G} for
\code{dimnames(arr)[[3]]}.  Defaults to \code{rownames(tbl)}.}
}
\value{
A 3-D numeric array \eqn{[d_1 \times d_2 \times G]}.
}
\description{
Reverses \code{\link[=arr_to_table]{arr_to_table()}}: given a data frame with one row per genotype
and \eqn{d_1 \cdot d_2} columns, returns a \eqn{[d_1 \times d_2 \times G]}
array.
}
\examples{
A <- array(rnorm(4 * 4 * 3), dim = c(4, 4, 3),
           dimnames = list(paste0("T", 1:4), paste0("T", 1:4),
                           paste0("G", 1:3)))
df <- arr_to_table(A)
# d1 / d2 inferred automatically from column names:
A2 <- table_to_arr(df)
all.equal(A, A2)  # TRUE

}
\seealso{
\code{\link[=arr_to_table]{arr_to_table()}}
}
