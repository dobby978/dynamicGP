% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconstruct.R
\name{reconstruct_dmd}
\alias{reconstruct_dmd}
\title{Multi-Step DMD State Reconstruction}
\usage{
reconstruct_dmd(
  A_arr,
  x0 = NULL,
  X_obs = NULL,
  B_arr = NULL,
  U_arr = NULL,
  n_steps = NULL,
  return_single_step = FALSE,
  plot_control_influence = TRUE,
  ctrl_factors = NULL,
  facet_by_factor = FALSE,
  operator_type = "discrete",
  ts_days = NULL,
  dt = NULL
)
}
\arguments{
\item{A_arr}{State-transition operator.  A 3-D array
\eqn{[p \times p \times G]} or a single \eqn{[p \times p]} matrix
(single-genotype convenience form).}

\item{x0}{Initial state vector or matrix.  Accepted forms:
\itemize{
\item A numeric vector of length \eqn{p} -- same starting state for
every genotype.
\item A \eqn{[p \times G]} matrix -- per-genotype starting states.
}
When \code{NULL} (default), the first time-column of \code{X_obs} is used.}

\item{X_obs}{Optional observed state array \eqn{[p \times N \times G]}
(or \eqn{[p \times N]} matrix for a single genotype).  Used to extract
\code{x0} when \code{x0 = NULL}, and as the driving states for single-step
reconstruction.}

\item{B_arr}{Optional control-input operator \eqn{[p \times q \times G]},
or a single \eqn{[p \times q]} matrix that is broadcast to all \code{G}
genotypes.}

\item{U_arr}{Optional control-input array \eqn{[q \times N_u \times G]},
or a single \eqn{[q \times N_u]} matrix that is broadcast to all \code{G}
genotypes.  Must be provided together with \code{B_arr}.}

\item{n_steps}{Integer number of prediction steps forward from \code{x0}.
Defaults to \code{ncol(X_obs) - 1} when \code{X_obs} is provided, or
\code{ncol(U_arr)} when only \code{U_arr} is provided.}

\item{return_single_step}{Logical.  If \code{TRUE} and \code{X_obs} is supplied,
compute and return \code{X_pred_1step_arr}.  Default \code{FALSE}.}

\item{plot_control_influence}{Logical.  If \code{TRUE} (default) and both
\code{B_arr} and \code{U_arr} are supplied, include heatmap(s) of the per-factor
signed control influence \eqn{B_{:,j}\,u_j(t)} per trait and time step
as \code{ctrl_plot}.}

\item{ctrl_factors}{Optional character vector of control-factor names or
integer indices selecting which columns of \code{U}/\code{B} to plot.
\code{NULL} (default) plots all \code{q} factors.  Ignored when
\code{plot_control_influence = FALSE}.}

\item{facet_by_factor}{Logical.  When multiple factors are selected,
combine all factor heatmaps into a single \pkg{ggplot2} plot faceted by
factor name (\code{TRUE}).  The default (\code{FALSE}) returns a named
list of individual heatmaps, one per factor.  Ignored when
\code{plot_control_influence = FALSE} or only one factor is selected.}

\item{operator_type}{Character; \code{"discrete"} (default) or \code{"continuous"}.
Set to \code{"continuous"} when \code{A_arr} and \code{B_arr} are the continuous-time
operators returned by \code{method = "continuous"} in
\code{\link[=run_dmd]{run_dmd()}}.  The continuous-time propagation uses the matrix
exponential with a zero-order-hold (ZOH) approximation for \code{B}:
\deqn{\hat{x}(t+\Delta t) = e^{A_c \Delta t}\hat{x}(t) +
        A_c^{-1}(e^{A_c \Delta t}-I)\,B_c\,u(t).}
Requires the \pkg{expm} package.}

\item{ts_days}{Numeric vector of time points (in days) of length
\eqn{n\_steps + 1} for multi-step reconstruction or \eqn{N} for
single-step reconstruction.  Used to compute per-step \eqn{\Delta t_k =
  ts\_days_{k+1} - ts\_days_k}.  Provide this \strong{or} \code{dt} when
\code{operator_type = "continuous"}.  Ignored for \code{"discrete"}.}

\item{dt}{Scalar uniform time step in days.  Used when \code{ts_days} is
\code{NULL} and \code{operator_type = "continuous"}.  Ignored for \code{"discrete"}.}
}
\value{
A named list containing one or more of:
\describe{
\item{\code{X_pred_arr}}{\eqn{[p \times (n\_steps+1) \times G]} multi-step
reconstruction.  Column 1 equals \code{x0}.}
\item{\code{X_pred_1step_arr}}{\eqn{[p \times (N-1) \times G]} single-step
predictions (present only when \code{return_single_step = TRUE} and
\code{X_obs} is supplied).}
\item{\code{ctrl_plot}}{Control-influence heatmap(s) for the selected
factor(s).  When a single factor is selected or
\code{facet_by_factor = TRUE}, a single \pkg{ggplot2} object.
Otherwise a named list of \pkg{ggplot2} objects, one per selected
factor.  Each heatmap shows the signed per-factor contribution
\eqn{B_{:,j}\,u_j(t)} to each trait at each time step, averaged
across genotypes.  Present only when \code{B_arr}, \code{U_arr},
and \code{plot_control_influence = TRUE}.}
}
}
\description{
Propagates state trajectories forward in time using fitted DMD operators.
Two modes are available:
}
\details{
\itemize{
\item \strong{Multi-step reconstruction} from an initial state \eqn{x_0}:
\deqn{\hat{x}(1) = x_0, \quad \hat{x}(t+1) = A\,\hat{x}(t) + B\,u(t)}
(discrete) or
\deqn{\hat{x}(t+\Delta t) = e^{A_c \Delta t}\hat{x}(t) +
        A_c^{-1}(e^{A_c \Delta t}-I)\,B_c\,u(t)}
(continuous, zero-order-hold for \eqn{B}).
This produces long-range forecasts and is always returned as
\code{X_pred_arr}.
\item \strong{Single-step (one-ahead) reconstruction} from the \emph{observed} state:
\deqn{\hat{x}(t+1) = A\,x_{\text{obs}}(t) + B\,u(t)}
(or its continuous-time analogue).
This measures the one-step model residual and is returned as
\code{X_pred_1step_arr} when \code{return_single_step = TRUE} and \code{X_obs} is
provided.
}
}
\seealso{
\code{\link[=run_dmd]{run_dmd()}}, \code{\link[=compare_arrays]{compare_arrays()}}, \code{\link[=plot_trajectories]{plot_trajectories()}}
}
