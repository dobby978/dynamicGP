% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{spectral_clipper}
\alias{spectral_clipper}
\title{Enforce Spectral Stability via Eigenvalue Clipping}
\usage{
spectral_clipper(A_all, epsilon = 1e-05)
}
\arguments{
\item{A_all}{Numeric matrix \verb{[p × p]} (single genotype) or 3-D array
\verb{[p × p × G]} (multiple genotypes) of state-transition operator matrices.}

\item{epsilon}{Numeric scalar or vector of length \code{G}.  Stability margin:
clipping threshold is \\eqn{1 - \\epsilon}. Default \code{0.00001} (very tight
margin from unit circle).  If a vector, element \code{i} applies to genotype \code{i}.}
}
\value{
A named list:
\\describe{
\\item{\code{A_stable_all}}{3-D array \verb{[p × p × G]} (or \verb{[p × p × 1]} for
matrix input) of stabilised state-transition operators, with
all eigenvalues guaranteed to satisfy \\eqn{|\\lambda_i| \\le 1 - \\epsilon}.}
\\item{\code{eigenvalues}}{Data frame with columns:
\\itemize{
\\item \code{Lambda}: original eigenvalue magnitude.
\\item \code{evs_clipped}: clipped eigenvalue magnitude.
\\item Column 3: genotype identifier.
}
Useful for diagnosing which eigenvalues were modified.}
}
}
\description{
Clips the eigenvalues of one or more state-transition operator matrices
to ensure spectral stability (i.e., all eigenvalues have magnitude strictly
less than 1 for discrete-time systems). This is useful for enforcing
physically-constrained dynamics when DMD operators are estimated
without explicit stability constraints.
}
\details{
For each eigenvalue \\eqn{\\lambda_i}, if \\eqn{|\\lambda_i| > 1 - \\epsilon},
it is rescaled to \\eqn{(1 - \\epsilon) \\cdot \\operatorname{sign}(\\lambda_i)}
(preserving argument/sign). Eigenvalues with \\eqn{|\\lambda_i| \\le 1 - \\epsilon}
are retained unchanged.
}
\seealso{
\code{\link[=run_dmd]{run_dmd()}}
}
